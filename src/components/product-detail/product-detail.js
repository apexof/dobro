$(document).ready(() => {
    $("#select2-product-detail-1").select2(
        {
            minimumResultsForSearch: -1,
            width: "100%",
            placeholder: "Выберите параметр"
        }
    );
});
